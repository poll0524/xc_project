<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.datatoolserver.mapper.billingImageMapper">
    <select id="selectBillingImage" resultType="com.example.datatoolserver.pojo.BillingImage">
        select * from billing_image where start = 1 limit #{pag},20;
    </select>
</mapper>