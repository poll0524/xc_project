<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.datatoolserver.mapper.courseRecordMapper">
    <insert id="insertShareCourse" parameterType="com.example.datatoolserver.pojo.ShareCourse">
        insert into share_course (open_id,course_id,start) values (#{openId},#{courseId},#{start});
    </insert>
</mapper>